name: ghcr.io/lesomnus/dev-gcc
args:
  CMAKE_VERSION: "3.24.2"

images:
  - tags: ["{{ .Major }}.{{ .Minor }}", "{{ .Major }}"]
    from:
      name: registry.hub.docker.com/library/gcc
      tag: '{ remoteTags | toSemver | semverMajorN 2 | semverMinorN 2 }'
